/**
 * reducer 总入口
 * 如果 reducer 文件太大，建议分成多个小的 reducer
 */

// import {
// } from './action_types';

function reducer(state, action) {
  let newState;

  switch (action.type) {
    // case ACTION_TYPE:
    //   break;
    default:
      break;
  }

  return newState;
}

module.exports = reducer;
